export enum Action {
  CREATE = "create",
  EDIT = "edit",
  APPLY = "apply",
}

export enum EntityName {
  FESTIVAL = "festival",
  APPLICATION = "application",
  RESIDENCY = "residency",
  VENUE = "venue",
}

export enum TagAction {
  STAR = "STAR",
  WATCH = "WATCH",
  INACTIVE = "INACTIVE",
  WARNING = "WARNING",
  OTHER = "OTHER",
}
